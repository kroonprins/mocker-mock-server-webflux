name: basic rule with templating
request:
  path: /templating
  method: GET
response:
  templatingEngine: mustache
  contentType: text/plain
  statusCode: 200
  body: |
    received a header 'h' with value {{req.query.q1}} and response status code of {{res.statusCode}} is set
