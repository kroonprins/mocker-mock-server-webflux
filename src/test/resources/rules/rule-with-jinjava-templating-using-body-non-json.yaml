name: rule with jinjava templating using input non-json body in templating
request:
  path: /templating-jinjava-non-json-input
  method: POST
response:
  templatingEngine: jinjava
  contentType: text/plain
  statusCode: 200
  body: |
    received non-json body: {{req.body}}